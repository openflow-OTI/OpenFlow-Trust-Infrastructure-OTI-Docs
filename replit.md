# OTI — Manager Workspace

This Replit workspace is the Development Manager's workspace for OTI (OpenFlow Trust Infrastructure), built by OpenFlow Labs (CEO: Ahmad). It contains only documentation — no source code. Source code lives in Ahmad's GitHub repos (see `extracted_docs/docs/MANAGER_HANDOVER.md`).

Full context, current state, and task queue live in:
- `extracted_docs/docs/MANAGER_HANDOVER.md` — briefing, production state, next actions
- `extracted_docs/docs/ARCHITECTURE.md` — codebase/infra ground truth
- `extracted_docs/docs/ROADMAP.md` — product vision and phases
- `extracted_docs/docs/TASKS.md` — master task queue

## User preferences

- Call him Ahmad — not "sir" or "boss".
- He works from his phone. Keep prompts in copy boxes, be direct and concise.
- One task at a time per Builder.
- **Give him one step/prompt at a time — do not bundle multiple next-step prompts or actions together in a single message. Wait for his go-ahead before preparing the next one.**
- Never touch `scoring.ts`, `nixpacks.toml`, or `vercel.json`.
- Never push to GitHub — Ahmad handles all Git operations himself.
- Builders only update task files when explicitly told to by the Manager.
- Always update `MANAGER_HANDOVER.md` and `TASKS.md` before ending a session.
